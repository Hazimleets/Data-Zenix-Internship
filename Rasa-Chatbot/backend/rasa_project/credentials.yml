# Rasa-Chatbot/rasa_project/credentials.yml

rest:
  # you can customize token auth here if desired

socketio:
  user_message_evt: user_uttered
  bot_message_evt: bot_uttered
  session_persistence: true
